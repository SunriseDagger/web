<!DOCTYPE html>
<html>
<head>
    <title>Список пользователей</title>
    <meta charset="utf-8" />
    <style>
        table {
            width: 100%;
            border: 4px double black; 
            border-collapse: collapse;
        }

        th {
            text-align: left; 
            background: #ccc;
            padding: 5px; 
            border: 1px solid black;
        }

        td {
            padding: 5px; 
            border: 1px solid black; 
        }
    </style>
</head>
<body>
    <h1>Здравствуйте</h1>
    <h2>Список пользователей</h2>
    <p><a href="/registration">Зарегестрировать пользователя</a></p>
    <table>
        <tr><th>Логин</th><th>Пароль</th><th>Статус пользователя (админ или нет)</th><th>
        {{#each users}}
        <tr>
            <td>{{this.login}}</td>
            <td>{{this.pass}}</td>
            <td>{{this.admin}}</td>
            <td>
                <a href="/edituser/{{this.id}}">Редактировать</a>|
                <form action="deleteuser/{{this.id}}" method="POST" style="display:inline;">
                    <input type="submit" value="Удалить" />
                </form>
            </td>
        </tr>
        {{/each}}
    </table>
    <h2>Список ботов</h2>
    <p><a href="/addbot">Добавить бота</a></p>
    <table>
        <tr><th>Идентификатор бота</th><th>Имя</th><th>Владелец</th><th>Статус (активен или нет)</th><th>
        {{#each botsconf}}
        <tr>
            <td>{{this.id}}</td>
            <td>{{this.name}}</td>
            <td>{{this.token}}</td>
            <td>{{this.status}}</td>
            <td>
                <a href="/editbot/{{this.id}}">Редактировать</a>|
                <form action="deletebot/{{this.id}}" method="POST" style="display:inline;">
                    <input type="submit" value="Удалить" />
                </form>
            </td>
        </tr>
        {{/each}}
    </table>
</body>
</html>
